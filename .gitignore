# Local .terraform directories
**/.terraform/*

# .tfstate files 
*.tfstate
*.tfstate.*

# Exclude all .tfvars files, which are likely to containe sentitive data, such as password, private key and other secrets.
*.tfvars

# Ignore CLI configuration files 
.terraformrc
terraform.rc

# Ignore Mac/OSX system file, with the folder metadata
.DS_Store
